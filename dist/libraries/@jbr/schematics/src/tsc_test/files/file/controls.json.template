<% if (controls) { %>
{
    "controls": [
    <% controls.forEach((ctrl, i) => { %>
        <%= stringify(ctrl, null, 2) %>
        <% if (i < controls.length - 1) { %>
        ,
        <% } %>
    <% }) %>
    ]
}
<% } %>
